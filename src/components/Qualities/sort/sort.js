import React from 'react'

const sort = () => {

  return (
    <select>
        <option></option>
        <option value={high}>Upvote high</option>
        <option value={low}>Upvote low</option>
    </select>
  )
}

export default sort;